import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: 'Abdulbasit Damilola Alabi | Portfolio',
  description: 'Software engineer with over 4 years of experience in developing robust web applications and driving user engagement.',
  keywords: 'software engineer, web development, portfolio, projects, experience',
  authors: [{ name: 'Abdulbasit Damilola Alabi', url: 'https://github.com/marrwan' }],
};

export default function RootLayout({
                                     children,
                                   }: Readonly<{
  children: React.ReactNode;
}>) {
  return (
      <html lang="en">
      <body
          className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
      {children}
      </body>
      </html>
  );
}
